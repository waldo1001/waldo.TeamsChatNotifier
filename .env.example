# Azure AD / Microsoft Entra ID
# Register a multi-tenant app at https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
# Supported account types: "Accounts in any organizational directory (Multi-tenant)"
# Required API permissions (Delegated):
#   - Chat.ReadBasic  (list chats)
#   - Chat.Read       (read messages)
#   - User.Read       (read signed-in user profile)
# No redirect URI needed for device code flow.

AZURE_CLIENT_ID=your-app-client-id-here
